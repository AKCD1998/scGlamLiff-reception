Place the built LIFF frontend bundle here for same-origin hosting under `/liff/`.

This directory is refreshed by `npm run sync:backend-liff` or `npm run build:backend-liff`.

Expected contents:
- `index.html`
- `assets/`
